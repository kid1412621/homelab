apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - namespace.yml
  - helmchartconfig.yml
  - serviceaccount.yml
  - basic-auth-middleware.yml
  - dashboard.yml
  - gatewayclass.yml
  - gateway.yml
  - gateway-clusterrole.yml
  - gateway-clusterrolebinding.yml
  - rolebinding.yml

# configMapGenerator:
#   - name: app-config
#     literals:
#       - APP_SUBDOMAIN=traefik

secretGenerator:
  - name: traefik-basic-auth
    type: Opaque
    envs:
      - .env
    options:
      disableNameSuffixHash: true
