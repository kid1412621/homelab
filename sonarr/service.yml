---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: sonarr
  name: service
spec:
  type: ClusterIP
  selector:
    app: sonarr
  ports:
    - name: http
      port: 8989
      targetPort: 8989
