---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: lidarr
  name: service
spec:
  type: ClusterIP
  selector:
    app: lidarr
  ports:
    - name: http
      port: 8686
      targetPort: 8686
